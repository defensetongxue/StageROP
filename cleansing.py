from ridgeSegModule import generate_ridge
def generate_crop(data_path):
    split
if __name__=='__main__':
    from config import get_config
    
    args=get_config()
    generate_ridge(args.path_tar)